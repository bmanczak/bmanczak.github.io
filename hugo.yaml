baseURL: "https://bmanczak.github.io/"
languageCode: "en-us"
title: "Blazej Manczak"
theme: "PaperMod"

# Publish into docs/ for GitHub Pages
publishDir: "public"

# Enable raw HTML in markdown
markup:
  goldmark:
    renderer:
      unsafe: true

# (optional) enable menu
menu:
  main:
    - identifier: "about"
      name: "About"
      url: "/about/"
      weight: 10
